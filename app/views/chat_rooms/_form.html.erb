<%= form_with(model: chat_room, local: true) do |form| %>
  <% if chat_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat_room.errors.count, "error") %> prohibited this chat_room from being saved:</h2>

      <ul>
      <% chat_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :senderable_id %>
    <%= form.text_field :senderable_id %>
  </div>

  <div class="field">
    <%= form.label :recieverable_id %>
    <%= form.text_field :recieverable_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
